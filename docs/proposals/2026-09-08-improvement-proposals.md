# Improvement Proposals — 2026-09-08

Generated by a scheduled sync-and-propose run, per
[issue #88](https://github.com/WintCom-Solutions/wintersoperations-site/issues/88)
(`ai-task` + `stage:plan`). These are proposals for Task Force review and
critique, not yet-approved work.

This scheduled sync found no actionable AI-assigned work pending: both open
PRs (#78, #65) are `blocked` + `needs:karl` on review-identity/build-gate
issues unrelated to code correctness, and neither open issue (#71
tracked-by-pr, #70 a separate Grok weakness-review track) is unclaimed build
work. The six proposals from the last two rounds (#49, #68) were all
approved and split into build issues (#71–#76); all appear resolved on
current `main` except #71, which is still stuck in review limbo rather than
awaiting further engineering work.

These three are not new — they were first written up in
`docs/proposals/2026-08-29-improvement-proposals.md`, but that round never
got its own tracking issue (unlike every round since), so Karl never had a
clean pursue/reject/defer decision point for it. Re-verified all three
against current `main` (`8aa7766`) before re-proposing here; nothing has
changed.

## 1. Wire the repo's own verification scripts and lint into CI

**Idea:** Add `npm run lint`, `npm run verify:metadata`, and
`npm run verify:contact` as steps in `.github/workflows/ci.yml`, alongside
the existing `npm ci` / `npm run build`.

**Rationale:** `package.json` already defines `verify:contact`
(`scripts/verify-contact.mjs`) and `verify:metadata`
(`scripts/verify-metadata.mjs`) — purpose-built checks for canonical URLs, OG
tags, and contact-form config across every route — plus a `lint` script, but
`ci.yml` (confirmed on current `main`) still only runs `npm ci` and
`npm run build`. A regression in `lib/site.ts` or `lib/content.ts` that
breaks a canonical tag or a Google Form entry ID currently builds and merges
cleanly with nothing catching it. This is the highest-leverage, lowest-risk
item of the three: no new tooling, just wiring up checks the repo already
wrote and is not running.

## 2. Fix the opaque `no-cors` contact-form submission path

**Idea:** In `components/ContactForm.tsx`, replace the `mode: "no-cors"`
fetch with a submission path that can actually distinguish success from
failure (a same-origin API route/proxy, or a provider response that isn't
opaque), and log a success/failure analytics event using the
`@vercel/analytics` integration already installed elsewhere in the app.

**Rationale:** `no-cors` mode (still line 47 on current `main`) makes the
response opaque, so a server-side rejection — a bad Google Form entry ID, a
Formspree quota hit — is indistinguishable from success, and
`setStatus("sent")` fires regardless. For a business site where the contact
form is a primary lead channel, a silently-swallowed submission failure is a
real business cost with no visibility: nothing currently logs it, despite
analytics already being wired up in `app/layout.tsx`.

## 3. Add automated tests for the two large interactive dashboard pages

**Idea:** Add Jest or Vitest + React Testing Library and cover
`app/itops-console/page.tsx` and `app/network-topology/page.tsx` first —
the two most complex, stateful client components in the app.

**Rationale:** There is still no `*.test.*`/`*.spec.*` file and no
test-runner config anywhere in the repo (confirmed: no `test`, `vitest`,
`jest`, or `playwright` entry in `package.json`). Those two pages carry
non-trivial `useState`/`useEffect`-driven interaction (simulated device
search, alerts, site selection), but the only existing automated checks are
the two Node scripts referenced in proposal #1, which assert on static HTML
output — not component behavior. A regression in either page's interaction
logic currently has no automated backstop at all.

## Not re-proposed

Two items the original doc flagged as content/ops tasks for a human, not
engineering work — still true, still not proposed as build work:

- README.md self-flags that the `wint-ops.com` → `wintersoperations.com`
  redirect must be configured at the host/registrar (static export, so
  `next.config.mjs` can't do it), and nothing in the repo tracks whether
  that was actually done.
- README.md also flags that the case studies in `lib/content.ts` are still
  drafts pending review against real engagements, despite being live on
  `/work`.

---

Feedback and critique welcome from any Task Force reviewer — including
disagreement that any of these are worth doing at all.
